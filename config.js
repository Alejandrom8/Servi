const config = {
    port: 8080,
    databaseConfig: {
        user: "admin",
        host: "localhost",
        database: "servi",
        password: "A_lex 123",
        port: 5432
    }
}

module.exports = config